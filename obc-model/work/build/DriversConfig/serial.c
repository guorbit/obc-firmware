/*
Code automatically generated by asn1scc tool
*/
#include <limits.h>
#include <string.h>
#include <math.h>


#include "serial.h"


const Serial_Conf_T linux_ttyS0 = {
    .devname = "/dev/ttyS0",
    .speed = b38400,
    .exist = {
        .speed = 1,
        .parity = 0,
        .bits = 0,
        .sending_wait = 0,
        .use_paritybit = 0
    }
};
const Serial_Conf_T linux_ttyUSB0 = {
    .devname = "/dev/ttyUSB0",
    .speed = b38400,
    .exist = {
        .speed = 1,
        .parity = 0,
        .bits = 0,
        .sending_wait = 0,
        .use_paritybit = 0
    }
};
const Serial_Conf_T linux_ttyUSB1 = {
    .devname = "/dev/ttyUSB1",
    .speed = b38400,
    .exist = {
        .speed = 1,
        .parity = 0,
        .bits = 0,
        .sending_wait = 0,
        .use_paritybit = 0
    }
};
const Serial_Conf_T linux_ttyUSB2 = {
    .devname = "/dev/ttyUSB2",
    .speed = b38400,
    .exist = {
        .speed = 1,
        .parity = 0,
        .bits = 0,
        .sending_wait = 0,
        .use_paritybit = 0
    }
};
const Serial_Conf_T leon_rasta_apburasta0 = {
    .devname = "/dev/apburasta0",
    .speed = b38400,
    .exist = {
        .speed = 1,
        .parity = 0,
        .bits = 0,
        .sending_wait = 0,
        .use_paritybit = 0
    }
};
const Serial_Conf_T leon_rasta_apburasta1 = {
    .devname = "/dev/apburasta1",
    .speed = b38400,
    .exist = {
        .speed = 1,
        .parity = 0,
        .bits = 0,
        .sending_wait = 0,
        .use_paritybit = 0
    }
};


flag Baudrate_T_IsConstraintValid(const Baudrate_T* pVal, int* pErrCode)
{
    flag ret = TRUE;
    ret = ((((((((((((*(pVal)) == b9600)) || (((*(pVal)) == b19200)))) || (((*(pVal)) == b38400)))) || (((*(pVal)) == b57600)))) || (((*(pVal)) == b115200)))) || (((*(pVal)) == b230400)));
    *pErrCode = ret ? 0 :  ERR_BAUDRATE_T; 

	return ret;
}

void Baudrate_T_Initialize(Baudrate_T* pVal)
{
	(void)pVal;


	(*(pVal)) = b9600;
}



flag Parity_T_IsConstraintValid(const Parity_T* pVal, int* pErrCode)
{
    flag ret = TRUE;
    ret = ((((*(pVal)) == even)) || (((*(pVal)) == odd)));
    *pErrCode = ret ? 0 :  ERR_PARITY_T; 

	return ret;
}

void Parity_T_Initialize(Parity_T* pVal)
{
	(void)pVal;


	(*(pVal)) = even;
}



flag Serial_Conf_T_devname_IsConstraintValid(const Serial_Conf_T_devname val, int* pErrCode)
{
    flag ret = TRUE;
    ret = ((1 <= strlen(val)) && (strlen(val) <= 20));
    *pErrCode = ret ? 0 :  ERR_SERIAL_CONF_T_DEVNAME; 

	return ret;
}

flag Serial_Conf_T_bits_IsConstraintValid(const Serial_Conf_T_bits* pVal, int* pErrCode)
{
    flag ret = TRUE;
    ret = ((7UL <= (*(pVal))) && ((*(pVal)) <= 8UL));
    *pErrCode = ret ? 0 :  ERR_SERIAL_CONF_T_BITS; 

	return ret;
}

flag Serial_Conf_T_sending_wait_IsConstraintValid(const Serial_Conf_T_sending_wait* pVal, int* pErrCode)
{
    flag ret = TRUE;
    ret = ((*(pVal)) <= 9000000UL);
    *pErrCode = ret ? 0 :  ERR_SERIAL_CONF_T_SENDING_WAIT; 

	return ret;
}

flag Serial_Conf_T_use_paritybit_IsConstraintValid(const Serial_Conf_T_use_paritybit* pVal, int* pErrCode)
{
    flag ret = TRUE;
	(void)pVal;
    ret = TRUE;
    *pErrCode = 0;

	return ret;
}

flag Serial_Conf_T_IsConstraintValid(const Serial_Conf_T* pVal, int* pErrCode)
{
    flag ret = TRUE;
    ret = Serial_Conf_T_devname_IsConstraintValid(pVal->devname, pErrCode);
    if (ret) {
        if (pVal->exist.speed) {
        	ret = Baudrate_T_IsConstraintValid((&(pVal->speed)), pErrCode);
        }
        if (ret) {
            if (pVal->exist.parity) {
            	ret = Parity_T_IsConstraintValid((&(pVal->parity)), pErrCode);
            }
            if (ret) {
                if (pVal->exist.bits) {
                	ret = Serial_Conf_T_bits_IsConstraintValid((&(pVal->bits)), pErrCode);
                }
                if (ret) {
                    if (pVal->exist.sending_wait) {
                    	ret = Serial_Conf_T_sending_wait_IsConstraintValid((&(pVal->sending_wait)), pErrCode);
                    }
                    if (ret) {
                        if (pVal->exist.use_paritybit) {
                        	ret = Serial_Conf_T_use_paritybit_IsConstraintValid((&(pVal->use_paritybit)), pErrCode);
                        }
                    }   /*COVERAGE_IGNORE*/
                }   /*COVERAGE_IGNORE*/
            }   /*COVERAGE_IGNORE*/
        }   /*COVERAGE_IGNORE*/
    }   /*COVERAGE_IGNORE*/

	return ret;
}

void Serial_Conf_T_devname_Initialize(Serial_Conf_T_devname val)
{
	(void)val;


	memset(val, 0x0, 21);

}
void Serial_Conf_T_bits_Initialize(Serial_Conf_T_bits* pVal)
{
	(void)pVal;


	(*(pVal)) = 7;
}
void Serial_Conf_T_sending_wait_Initialize(Serial_Conf_T_sending_wait* pVal)
{
	(void)pVal;


	(*(pVal)) = 0;
}
void Serial_Conf_T_use_paritybit_Initialize(Serial_Conf_T_use_paritybit* pVal)
{
	(void)pVal;


	(*(pVal)) = FALSE;
}
void Serial_Conf_T_Initialize(Serial_Conf_T* pVal)
{
	(void)pVal;



	/*set devname */
	Serial_Conf_T_devname_Initialize(pVal->devname);
	/*set speed */
	pVal->exist.speed = 1;
	Baudrate_T_Initialize((&(pVal->speed)));
	/*set parity */
	pVal->exist.parity = 1;
	Parity_T_Initialize((&(pVal->parity)));
	/*set bits */
	pVal->exist.bits = 1;
	Serial_Conf_T_bits_Initialize((&(pVal->bits)));
	/*set sending_wait */
	pVal->exist.sending_wait = 1;
	Serial_Conf_T_sending_wait_Initialize((&(pVal->sending_wait)));
	/*set use_paritybit */
	pVal->exist.use_paritybit = 1;
	Serial_Conf_T_use_paritybit_Initialize((&(pVal->use_paritybit)));
}

